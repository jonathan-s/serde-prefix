[package]
name = "serde_prefix"
version = "0.1.0"
authors = ["Jonathan Sundqvist <jonathan@argpar.se>"]
edition = "2018"

[lib]
proc-macro = true

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
syn = { version = "*", features = ["full"] }
quote = "*"
